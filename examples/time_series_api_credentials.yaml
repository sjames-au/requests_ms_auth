
# The Azure resource ID  [required]
resource: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# The Azure tenant ID  [required]
tenant: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# The client ID  [required]
client_id: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# The client secret  [required]
client_secret: "XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# An endpoint that should return 200 on get with these credentials [optional]
verification_url: "https://api.gateway.equinor.com/plant/timeseries/v1.5"

# A json element name that should be in the top level of the returned [optional]
verification_element: "data"

